spring.application.name=servicio-instructor

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:instructor_db}?useSSL=false

spring.datasource.username=${DB_USER:app_user}
spring.datasource.password=${DB_PASSWORD:123}

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
